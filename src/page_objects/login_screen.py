class LoginPage():
    user_input = "some-id"
    password_input = "some-class"
    submit_btn = "some-xpath"

    def enter_username(self, username):
        #logger.debug("Entering username")
        try:
            #driver.findElement(By.id(user_input).sendText(username)
            pass
        except:
            #logger.error("could not eneter username" + error)
            pass


    def enter_password(self, password):
        #driver.findElement(By.class(password_input)).sendText(password)
        pass

    def submit_form(self):
        #driver.findElement(By.xpath(submit_btn)).tap()
        pass

